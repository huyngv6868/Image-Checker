# Gemini API key (free) — lấy tại https://aistudio.google.com/apikey
# Nhiều key cách nhau dấu phẩy để xoay vòng
GEMINI_API_KEYS=your_key_here

# Model (mặc định dùng Flash — free tier)
GEMINI_MODEL=gemini-2.5-flash
